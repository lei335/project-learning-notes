VDF函数：加密很慢，解密很快。例如（sloth函数）
非拜占庭容错：只存在是否接收到信息问题。
拜占庭容错：不仅有是否接收的问题，还存在作恶节点的问题，即节点发送出去的信息不是自己接收到的信息。
PDFT（实用拜占庭容错算法）：可以在作恶节点少于三分之一的情况下，保证系统的正确性。


hash算法：是密码学里的经典技术，可以用来验证有没有人篡改数据。


用公私钥加密一般很慢，所以用对称加密。比如aes加密，但是用同一个密钥加密不是很好，用私钥处理每个数据块的Id,得到的aes不一样。更利于共享，只用分享对称加密的密码。（aes密钥生成）
